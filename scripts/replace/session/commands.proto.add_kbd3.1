    // Use special table for Godan (to hiragana).
    GODAN_TO_HIRAGANA = 30;

    // Use special table for Godan (to half-width ascii).
    GODAN_TO_HALFWIDTHASCII = 31;

    // Use special table for Godan (to number).
    GODAN_TO_NUMBER = 32;

    // Use special table for niko flick (to hiragana).
    NIKO_FLICK_TO_HIRAGANA = 116;

    // Use special table for niko flick (to half-width ascii).
    NIKO_FLICK_TO_HALFWIDTHASCII = 117;

    // Use special table for niko flick (to number).
    NIKO_FLICK_TO_NUMBER = 118;

    // Use special table for bell flick (to hiragana).
    BELL_FLICK_TO_HIRAGANA = 216;

    // Use special table for bell flick (to half-width ascii).
    BELL_FLICK_TO_HALFWIDTHASCII = 217;

    // Use special table for bell flick (to number).
    BELL_FLICK_TO_NUMBER = 218;
